<html>
<body>
<div>
    Submitter name: ${submitter.firstName} ${submitter.surname} (Database ID: ${submitter.submitterId})<br>
    Submitter email address: ${submitter.emailAddress}<br>
    Submission title: ${subForm.subTitle}<br>
    Release date: ${subForm.releaseDate}<br>
    Data description: ${subForm.dataDesc}<br>
    Is analysis required: <br>
    Is Human associated: ${subForm.humanAssociated}<br>
    Test:
    #if( ${subForm.analysisRequired} )
        yes
    #else
        no
    #end
</div>
</body>
</html>