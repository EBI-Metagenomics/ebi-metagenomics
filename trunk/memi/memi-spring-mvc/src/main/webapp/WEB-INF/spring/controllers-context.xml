<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:tx="http://www.springframework.org/schema/tx" xmlns:aop="http://www.springframework.org/schema/aop"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
		http://www.springframework.org/schema/mvc
		http://www.springframework.org/schema/mvc/spring-mvc-3.0.xsd
		http://www.springframework.org/schema/context
		http://www.springframework.org/schema/context/spring-context-3.0.xsd
		http://www.springframework.org/schema/tx
		http://www.springframework.org/schema/tx/spring-tx-3.0.xsd
		http://www.springframework.org/schema/task
		http://www.springframework.org/schema/task/spring-task-3.0.xsd
		http://www.springframework.org/schema/aop
		http://www.springframework.org/schema/aop/spring-aop-2.0.xsd">

    <!-- Scans within the base package of the application for @Components to configure as beans -->
    <context:component-scan base-package="uk.ac.ebi.interpro.metagenomics.memi"/>

    <tx:annotation-driven transaction-manager="txManager"/>

    <bean id="txManager" class="org.springframework.orm.hibernate3.HibernateTransactionManager">
        <property name="sessionFactory" ref="hibernateSessionFactory"/>
    </bean>

    <!--Data source for IPDEV-->
    <bean id="ipDevDatasourceId" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName" value="oracle.jdbc.driver.OracleDriver"/>
        <property name="url" value="${jcp.jdbc.oracle.ipdev.url}"/>
        <property name="username" value="${jcp.jdbc.oracle.ipdev.username}"/>
        <property name="password" value="${jcp.jdbc.oracle.ipdev.password}"/>
    </bean>

    <!--Data source for ADEV-->
    <bean id="adevDatasourceId" name="adevDatasource"
          class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName" value="oracle.jdbc.driver.OracleDriver"/>
        <property name="url" value="${jcp.jdbc.oracle.adev.url}"/>
        <property name="username" value="${jcp.jdbc.oracle.adev.username}"/>
        <property name="password" value="${jcp.jdbc.oracle.adev.password}"/>
    </bean>

    <!-- Definition of Hibernate session factory including Hibernate Mappings-->
    <bean id="hibernateSessionFactory"
          class="org.springframework.orm.hibernate3.annotation.AnnotationSessionFactoryBean">
        <property name="dataSource" ref="ipDevDatasourceId"/>
        <property name="hibernateProperties">
            <props>
                <prop key="hibernate.dialect">org.hibernate.dialect.Oracle10gDialect</prop>
                <prop key="hibernate.show_sql">true</prop>
                <prop key="hibernate.cache.provider_class">org.hibernate.cache.NoCacheProvider</prop>
                <prop key="hibernate.cache.use_query_cache">false</prop>
                <prop key="hibernate.connection.pool_size">20</prop>
                <!--<prop key="hibernate.hbm2ddl.auto">update</prop>-->
            </props>
        </property>
        <!--<property name="location" value="classpath:resources/hibernate.properties"/>-->
        <property name="annotatedClasses">
            <list>
                <value>uk.ac.ebi.interpro.metagenomics.memi.model.EmgSample</value>
                <value>uk.ac.ebi.interpro.metagenomics.memi.model.EmgStudy</value>
                <value>uk.ac.ebi.interpro.metagenomics.memi.model.hibernate.Publication</value>
                <value>uk.ac.ebi.interpro.metagenomics.memi.model.hibernate.Study</value>
                <value>uk.ac.ebi.interpro.metagenomics.memi.model.hibernate.Sample</value>
                <value>uk.ac.ebi.interpro.metagenomics.memi.model.hibernate.EnvironmentSample</value>
                <value>uk.ac.ebi.interpro.metagenomics.memi.model.hibernate.HostSample</value>
            </list>
        </property>

    </bean>


    <!-- Specification of version of control elements like e.g. data access objects -->
    <!-- List of data access objects -->
    <bean id="emgStudyDAO" class="uk.ac.ebi.interpro.metagenomics.memi.dao.EmgStudyDAOImpl"/>
    <bean id="studyDAO" class="uk.ac.ebi.interpro.metagenomics.memi.dao.HibernateStudyDAOImpl"/>

    <bean id="emgSampleDAO" class="uk.ac.ebi.interpro.metagenomics.memi.dao.EmgSampleDAOImpl"/>
    <bean id="sampleDAO" class="uk.ac.ebi.interpro.metagenomics.memi.dao.HibernateSampleDAOImpl"/>

    <bean id="submitterDAOId" name="submitterDAO" class="uk.ac.ebi.interpro.metagenomics.memi.dao.SubmitterDAOImpl">
        <property name="dataSource" ref="adevDatasource"/>
    </bean>

    <bean id="newsDAO" class="uk.ac.ebi.interpro.metagenomics.memi.dao.NewsDAOImpl"/>

    <bean id="messageSource" class="org.springframework.context.support.ReloadableResourceBundleMessageSource">
        <property name="basename" value="/WEB-INF/resources/messages"/>
    </bean>

    <!--Service-->

    <!-- The next 3 beans belong to the email notification service which is currently used in the submission form  -->
    <bean id="mailSender" class="org.springframework.mail.javamail.JavaMailSenderImpl">
        <property name="host" value="smtp.ebi.ac.uk"/>
        <property name="port" value="25"/>
    </bean>

    <!-- This bean initializes an email notification service -->
    <bean id="emailNotificationServiceId" name="emailNotificationService"
          class="uk.ac.ebi.interpro.metagenomics.memi.services.EmailNotificationService">
        <constructor-arg ref="mailSender"/>
    </bean>

    <!-- This bean initializes the Velocity engine for templating. -->
    <bean id="velocityEngineId" name="velocityEngine" class="org.springframework.ui.velocity.VelocityEngineFactoryBean">
        <property name="velocityProperties">
            <props>
                <prop key="resource.loader">class</prop>
                <prop key="class.resource.loader.class">
                    org.apache.velocity.runtime.resource.loader.ClasspathResourceLoader
                </prop>
            </props>
        </property>
        <property name="resourceLoaderPath">
            <value>/WEB-INF/velocity_templates</value>
        </property>
    </bean>

    <!--This service configures a HTTP response, which opens a download dialog-->
    <bean id="downloadService" class="uk.ac.ebi.interpro.metagenomics.memi.services.MemiDownloadService"/>

    <!-- The session bean holds the current logged in user/submitter reference-->
    <!-- an HTTP Session-scoped bean exposed as a proxy -->
    <bean id="sessionBeanRef" class="uk.ac.ebi.interpro.metagenomics.memi.springmvc.session.SessionBean"
          scope="session">
        <!-- this next element effects the proxying of the surrounding bean -->
        <aop:scoped-proxy/>
    </bean>

    <!-- a singleton-scoped bean injected with a proxy to the above bean -->
    <bean id="sessionManager" class="uk.ac.ebi.interpro.metagenomics.memi.springmvc.session.SessionManager">
        <!-- a reference to the proxied userPreferences bean -->
        <property name="sessionBean" ref="sessionBeanRef"/>
    </bean>

</beans>