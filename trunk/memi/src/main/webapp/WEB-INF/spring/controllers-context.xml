<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
		http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-3.0.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.0.xsd">

    <!-- Scans within the base package of the application for @Components to configure as beans -->
    <context:component-scan base-package="uk.ac.ebi.interpro.metagenomics.memi"/>

    <!-- Specification of version of control elements like e.g. data access objects -->
    <!-- List of data access objects -->
    <bean id="submitterDAOId" name="submitterDAO" class="uk.ac.ebi.interpro.metagenomics.memi.dao.SubmitterDAOImpl">
        <property name="dataSource" ref="adevDatasource"/>
    </bean>

    <bean id="studyDAO" class="uk.ac.ebi.interpro.metagenomics.memi.dao.StudyDAOImpl"/>

    <bean id="newsDAO" class="uk.ac.ebi.interpro.metagenomics.memi.dao.NewsDAOImpl"/>

    <bean id="sampleDAO" class="uk.ac.ebi.interpro.metagenomics.memi.dao.SampleDAOImpl"/>

    <!-- Data source for the Oracle ENA database -->
    <bean id="adevDatasourceId" name="adevDatasource"
          class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName" value="${jcp.jdbc.oracle.driverClassName}"/>
        <property name="url" value="${jcp.jdbc.oracle.adev.url}"/>
        <property name="username" value="${jcp.jdbc.oracle.adev.username}"/>
        <property name="password" value="${jcp.jdbc.oracle.adev.password}"/>
    </bean>

    <bean id="messageSource" class="org.springframework.context.support.ReloadableResourceBundleMessageSource">
        <property name="basename" value="/WEB-INF/resources/messages"/>
    </bean>

    <!-- The next 3 beans belong to the email notification service which is currently used in the submission form  -->
    <bean id="mailSender" class="org.springframework.mail.javamail.JavaMailSenderImpl">
        <property name="host" value="smtp.ebi.ac.uk"/>
        <property name="port" value="25"/>
    </bean>

    <!-- This bean initializes an email notification service -->
    <bean id="emailNotificationServiceId" name="emailNotificationService"
          class="uk.ac.ebi.interpro.metagenomics.memi.services.EmailNotificationService">
        <constructor-arg ref="mailSender"/>
    </bean>

    <!-- This bean initializes the Velocity engine for templating. -->
    <bean id="velocityEngineId" name="velocityEngine" class="org.springframework.ui.velocity.VelocityEngineFactoryBean">
        <property name="velocityProperties">
            <value>
                resource.loader=class
                class.resource.loader.class=org.apache.velocity.runtime.resource.loader.ClasspathResourceLoader
            </value>
        </property>
        <property name="resourceLoaderPath">
            <value>/WEB-INF/templates</value>
        </property>
    </bean>

</beans>